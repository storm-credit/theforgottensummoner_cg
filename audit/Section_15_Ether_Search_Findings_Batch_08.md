# Section 15 Ether Search Findings - Batch 08 Snapshot

이 문서는 에테르 후보명 탐색 큐 8번,
`포트 넥서스 / 제3 부두 / 검은 고양이 / 머시너리 게이트 / 그늘항로 기록관` 축의 검색 결과다.

## Scope

검색 범위:

- `working/imports`
- `audit`

검색어:

- `포트 넥서스`
- `Port Nexus`
- `제3 부두`
- `검은 고양이`
- `머시너리 게이트`
- `Mercenary Gate`
- `계약문`
- `그늘항로 기록관`
- `서약문 보관관`

## Findings

| Target | Evidence | Updated Judgment | Note |
|---|---|---|---|
| `포트 넥서스` | 대륙 간 무역의 최대 관문, 연합 해군 본부, 밀수 허브로 확인된다. 항만청, 제1부두, 빈민가 `쥐구멍`이 보인다. | `place_slot_confirmed` | 항만/밀수/검문/밀항 장소-기관 슬롯. |
| `제3 부두` | 현재 검색 범위에서는 직접 확인되지 않는다. | `not_found_in_current_batch` | 기존 자유도시 지도/운영 매트릭스와 원본 전체 확인 필요. |
| `검은 고양이` | 현재 검색 범위에서는 직접 확인되지 않는다. | `not_found_in_current_batch` | 기존 자유도시 지도/운영 매트릭스와 원본 전체 확인 필요. |
| `드레이크 루가` | 포트 넥서스 제독/해군 사령부 지배자로 보이며, 14 현존 영웅 파일이 직접 확인된다. | `keep_14` | 15 후보가 아니라 자유도시연합 14 핵심 영웅으로 유지. |
| `셀레나 와일드웨이브` | 포트 넥서스 정보원, 그림자 조합 지휘자, 뒷골목 지배자, 밀항 루트 제공자로 보인다. | `verify_before_15 / name_collision_check` | 현재 14 작업본에는 `셀레나 아르시엔`이 따로 있어 이름 충돌/별도 인물 여부 확인 필요. |
| `조선소장` | 포트 넥서스 핵심 인물 슬롯에서 전설적인 선박 기술자로 보인다. | `need_named_candidate` 유지 | 개인명 없음. 해양/항만 장인 명사 슬롯으로 보존. |
| `항만청` | 입출항 관리, 세금 징수, 뇌물 가능성이 보이는 항만 행정 기관이다. | `institution_slot_confirmed` | 항만 인장관/청동 봉인관 계열과 연결 가능. |
| `머시너리 게이트` | 자유도시연합 군사적 심장, 용병 훈련/파견, 국경 수비 요새로 확인된다. | `place_slot_confirmed` | 용병 조합/등급 심사/계약 장소-기관 슬롯. |
| `레온 벨가르드` | 머시너리 게이트 조합장, 용병 조합 지배자, 멘토/넘어야 할 산으로 보인다. | `verify_before_15` 유지 | 강한 named notable 후보지만 조합장/지배자라 14 또는 세력 핵심 여부 확인 전 확정 금지. |
| `교관` | 머시너리 게이트 핵심 인물 슬롯에서 호랑이 교관으로 보인다. | `need_named_candidate` 유지 | 개인명 없음. 용병 훈련 명사 슬롯으로 보존. |
| `황금 맹약의 신전` | 상업과 계약의 신 성소, 대륙 공증 사무소, 계약 원본 지하 서고, 집행관 조직이 확인된다. | `institution_slot_confirmed` | `서약문 보관관`의 강한 장소/기관 앵커. |
| `대집행관` | 황금 맹약의 신전 핵심 인물 슬롯에서 법전으로 사람을 패는 무투파 사제로 보인다. | `need_named_candidate` 유지 | 개인명 없음. 계약 집행/공증 명사 슬롯. |
| `그늘항로 기록관` | 표면명 후보로는 유효하지만 현재 검색 범위에서 별도 개인명은 확인되지 않는다. | `need_named_candidate` 유지 | 포트 넥서스 항만/밀수/밀항 루트 관리 슬롯. |
| `서약문 보관관` | 표면명 후보로는 유효하지만 현재 검색 범위에서 별도 개인명은 확인되지 않는다. | `need_named_candidate` 유지 | 황금 맹약의 신전 지하 서고/공증 문서 슬롯. |

## Collision Notes

`드레이크 루가`는 포트 넥서스 관련 문서에서 `Drake Ruga`와 `Drake Rawson` 표기가 함께 보인다.
현재는 14 현존 영웅 파일명인 `드레이크 루가 (Drake Ruga)`를 우선 앵커로 둔다.

`셀레나 와일드웨이브`는 자유도시 정보원/그림자 조합 축으로 보이지만,
현재 14 작업본에는 `셀레나 아르시엔`이 별도 확인된다.
이름만 보고 병합하지 않는다.

## Conductor Reading

이 검색 기록에서는 새 15 확정자가 확인되지 않았다.

- `드레이크 루가`는 `keep_14`.
- `셀레나 와일드웨이브`와 `레온 벨가르드`는 `verify_before_15`.
- `조선소장`, `교관`, `대집행관`, `그늘항로 기록관`, `서약문 보관관`은 개인명 없는 `need_named_candidate` 슬롯으로 유지.
- `포트 넥서스`, `머시너리 게이트`, `황금 맹약의 신전`은 자유도시 장소-기관 슬롯으로 보존.

## Queue Snapshot

큐 9번으로 넘어간다.

후속 reference 대상:

- `루미라`
- `엘다라`
- `정령 계약`
- `잠든 정령의 숲`
- `정령의 무덤`
- `이름 새김`
- `정령서약 해석자`
- `정령묘 이름새김꾼`
