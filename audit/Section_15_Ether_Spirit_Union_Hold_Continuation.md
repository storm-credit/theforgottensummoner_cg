# Section 15 Ether Spirit-Union Hold Snapshot

목적:

- `드라이덴 / 메라 / 실라스 나이트쉐이드` 3인을 정령연합 spirit-union hold 묶음으로 유지한다.
- 이미 흩어져 있는 `great_druid`, `name_drift`, `shadow-assassin` 압력을 `Section 15` 문서군에서 같은 강도로 읽히게 한다.
- 정령연합 내부에서도 `멘토-정책`, `외교-조약`, `암살-그늘항로` 축을 평평하게 섞지 않고 분리한다.

## Snapshot

| Candidate | Core Read | State Snapshot | Main Pressure |
|---|---|---|---|
| `드라이덴 썬더루트` | `엘더 루트의 뿌리의 수호자 / 생명의 의회 의장 / 대드루이드` | `verify_before_15 / great_druid_hold` | `Top 3 + S급 + Act + 생명의 의회 정책 코어` |
| `메라 라일윈드` | `정령연합 외교 사절단 / 희귀 재료 교역 조건 관리자` | `verify_before_15 / spirit_envoy_hold / name_collision_watch` | `Top 4 외교관 + 교역 조건 + Mera drift` |
| `실라스 나이트쉐이드` | `그늘까마귀단 단장 / 잠든 정령의 숲 그림자 암살자` | `verify_before_15 / shadow_crow_hold / name_collision_watch` | `No.12 + S급 + 그늘까마귀단 + Sylas collision` |

## Recheck Outcome

| Candidate | Direct 14 File In Current Repo | Conductor Decision | Note |
|---|---|---|---|
| `드라이덴 썬더루트` | `no_direct_14_match` | `verify_before_15 / great_druid_hold` | 엘더 루트, 생명의 의회, 정령연합 관계도에서 `뿌리의 수호자`, `정책 조언자`, `Top 3 대드루이드`, `S급`, `Act` 신호가 반복된다. |
| `메라 라일윈드` | `different_14_name_match_exists` | `verify_before_15 / spirit_envoy_hold / name_collision_watch` | 현 범위에서 `메라 라일윈드` 독립 14 파일은 직접 보이지 않지만, 14 작업본의 `메라 실피드`가 별도 앵커로 확인된다. 외교·교역 조건 명사 가치는 크지만 이름 드리프트를 먼저 잠근다. |
| `실라스 나이트쉐이드` | `separate_anchor_neighbor_exists` | `verify_before_15 / shadow_crow_hold / name_collision_watch` | `실라스 블랙쏜`과는 세력 앵커가 다르고, `잠든 정령의 숲 / 그늘까마귀단 / S급 암살자` 압력이 강하다. 암살/첩보 명사 가치보다 14 또는 세력 핵심 압력이 먼저다. |

## Internal Read

- 14 crossover 압력은 `드라이덴 > 실라스 > 메라`로 읽는다.
- 남는 15 Named Notable 가치만 보면 `메라 > 드라이덴 > 실라스`로 읽는다.
- 정령연합 내부 기능 분리는 `드라이덴 = mentor-policy`, `메라 = envoy-trade`, `실라스 = shadow-crow enforcement`다.

## Conductor Note

- 현재 이 문서는 새 후보 추가가 아니라 `정령연합 hold 축 정밀화` snapshot reference다.
- `드라이덴`은 자연법 해석자라는 명사 가치보다 `생명의 의회 / 대드루이드 / 멘토` 압력이 먼저라 `great_druid_hold`로 둔다.
- `메라`는 외교·교역 조건 명사 가치가 높아도 `메라 실피드` 드리프트를 풀기 전 15 고정으로 올리지 않고 `spirit_envoy_hold`로 둔다.
- `실라스 나이트쉐이드`는 `실라스 블랙쏜`과 병합하지 않고 `그늘까마귀단 / 잠든 정령의 숲` 축으로만 읽는다.
- 현재 본선은 새 에테르 hold 확장을 전제하지 않고 `5대륙 closure sync / Section 8 -> 15 watch-reference`를 유지하는 쪽으로 읽는다.
