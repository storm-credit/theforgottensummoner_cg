# Section 15 Oceanic Search Findings Batch 03 Snapshot

이 문서는 해양 `15 Named Notables` 후보명 탐색의
우선 5슬롯 read-only closure 결과를 기록한다.

## Search Scope

검색 범위:

- `working/imports/phase4_section8_oceanic_church_of_sea`
- `working/imports/phase4_section8_oceanic_golden_armada`
- `working/imports/phase4_section8_oceanic_pirate_confederacy`

핵심 앵커:

- `오라클 바지 (Oracular Barge)`
- `골든 앵커 하버 (Golden Anchor Harbor)`
- `크로스윈드 포트 (Crosswind Port)`
- `블랙워터 항구 (Blackwater Cove)`
- `붉은 해골 섬 (Crimson Skull Isle)`
- `볼트 오브 아우룸 (Vault of Aurum)`

## Findings

| Search Slot | Best Anchor File(s) | Evidence Found | Recorded State | Recorded Judgment |
|---|---|---|---|---|
| `수석 오라클 / 파도 신탁장` | `오라클 바지 (Oracular Barge)` | `수석 오라클`과 `그녀를 호위하는 12명의 타이드워커`가 확인되지만 개인명은 없다. | `no_named_candidate_yet` | 직함은 강하지만 direct holder는 미확인. `신탁 방주` 슬롯 유지. |
| `오라클 바지 관련 이름 후보` | `오라클 바지`, `전설적 마도 영웅록`, `성스러운 함대` | `세일블레스 마리아`와의 연결은 보이지만 `A급`, `성스러운 함대 제독` 신호가 강하다. | `boundary_candidate_only` | `수석 오라클` 실명으로 쓰지 않는다. 14/15 경계 후보로만 보존. |
| `항로 기록관 / 해로 장부관 / 청해도 보관관` | `골든 앵커 하버`, `크로스윈드 포트` | 항로 배치, 호위 스케줄, 보험 요율, `항해사 길드 본부`, `기술 데이터 거래`, `해도` 기능이 반복되지만 named officer는 없다. | `role_slot_confirmed` | 항로/해도/장부 기능 슬롯 유지. `마르코 바르텔로` 같은 상업 축 이름을 direct holder로 덮어씌우지 않는다. |
| `왕실 조선소 수석 공병 / 왕실 선공장 수석장` | `크로스윈드 포트` | `제1 국영 조선소`, `조선 지구`, `마스터 쉽라이트`, `수석 기상관`, `항해사 길드장` 슬롯이 보이지만 수석 공병 실명은 없다. | `role_slot_confirmed` | 왕실 조선소 책임자 슬롯은 강하지만 direct holder는 미확인. role slot 유지. |
| `장물 감정사 / 흑조 감정관` | `블랙워터 항구`, `붉은 해골 섬`, `해적 연합 마법 체계` | `늙은 감정사`, `유물 감정`, `진품 감정`, `검은 도가니`의 강제 감정사 노동이 보이지만 stable named holder는 없다. | `role_slot_confirmed` | 감정 기능은 강하게 확인되지만 개인명은 없다. `골드핑거 바스` 같은 암시장 권력축과 병합하지 않는다. |
| `심해 금고 보관인 / 심연 장부관` | `볼트 오브 아우룸`, `황금 함대 마법 체계 및 연구 기관` | `7중 금고`, `계약의 방`, `재난 대비 금고`, `재무관청 직할`, `이소벨 재무관청의 결재`는 보이지만 금고/장부 direct holder는 없다. | `boundary_candidate_only` | 금고/장부 apparatus는 강하지만 named custodian은 없다. `이소벨 골드리프`는 SS급 재무 권력축 경계 후보로만 분리 유지. |

## Closure Evidence Summary

- 우선 5슬롯 모두 원본 import 기준 `direct named holder`는 확인되지 않았다.
- `수석 오라클`, `해로 장부관`, `왕실 선공장 수석장`, `흑조 감정관`은 역할 슬롯 근거가 충분하다.
- `세일블레스 마리아`, `이소벨 골드리프`는 슬롯 실명이 아니라 경계 후보 쪽이 더 강하다.

## Queue Snapshot

후속 reference는 해양 2열 도시 실무층으로 이동하는 흐름으로 읽는다.

1. `조선공 길드 장인 / 검은돛 선장인`
2. `항해사 길드장`
3. `마스터 쉽라이트`
4. `수석 기상관`
5. `대경매장 주인`
6. `은행장`
7. `세관장`

`진혼 악기지기`, `망자항해 기록관`, `검은돛 선장인`은
도시 실무층 pass 뒤의 후속 슬롯으로 유지한다.
